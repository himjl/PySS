from setuptools import setup
from setuptools import setup, find_packages

setup(name='PySS',
      version='0.01',
      description='State-space model fits written in Python',
      url='https://github.com/mlee3142/PySS',
      author='mil',
      author_email='none@none.com',
      license='MIT',
      packages=find_packages(),
      install_requires=[],
      zip_safe=False)
